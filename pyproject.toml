[project]
name = "hermes"
version = "0.1.0"
description = "Hermes — A-stock analysis, screening, and portfolio tracking CLI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "akshare>=1.18.0",
    "ddgs>=9.14.2",
    "duckduckgo-search>=8.1.1",
    "lxml>=6.1.0",
    "mcp>=1.27.1",
    "requests>=2.33.1",
    "typer>=0.15.0",
]

[project.scripts]
hermes = "hermes.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hermes"]